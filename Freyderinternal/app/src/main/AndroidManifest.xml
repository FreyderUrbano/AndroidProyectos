<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.internallogin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InternalLogin">
        <activity
            android:name=".Lienzo"
            android:label="@string/title_activity_lienzo"
            android:theme="@style/Theme.InternalLogin.NoActionBar"></activity>
        <activity
            android:name=".Configuracion"
            android:label="@string/title_activity_configuracion"
            android:theme="@style/Theme.InternalLogin.NoActionBar" />
        <activity
            android:name=".Login"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.InternalLogin.NoActionBar" />
        <activity
            android:name=".Wellcome"
            android:label="@string/title_activity_wellcome"
            android:theme="@style/Theme.InternalLogin.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.InternalLogin.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>